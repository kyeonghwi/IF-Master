services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ifmaster
      POSTGRES_USER: ifmaster
      POSTGRES_PASSWORD: ifmaster
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
